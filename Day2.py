# Create a program to calculate the area of circule given its radius

import math

radius = float(input("enter radius of circule : "))

area = math.pi * radius ** 2

print("raduis of circule = {} and its area = {}".format(radius, area))